#%RAML 0.8 ResourceType

type: collection
get:
  responses:
    200:
      body:
        application/json:
          schema: <<collectionSchema>>
          example: <<getRespExample>>
post:
  body:
    application/json:
      schema: <<schema>>
      example: <<reqExample>>
  responses:
    201:
      headers:
          Location: 
            description: Location of the newly created entity
      body:
        application/json:
          schema: <<schema>>
          example: <<respExample>>