<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- enable use-expressions -->
	<http auto-config="true" use-expressions="true">
		<!-- <intercept-url pattern="/loginPage" access="!isAuthenticated()" /> -->
		<!-- <intercept-url pattern="/loginPage" access="permitAll" /> -->
		<intercept-url pattern="/home" access="hasAnyRole('ROLE_USER','ROLE_ADMIN')" />
		<intercept-url pattern="/home/**" access="hasAnyRole('ROLE_USER','ROLE_ADMIN')" />
		<intercept-url pattern="/person" access="hasAnyRole('ROLE_USER','ROLE_ADMIN')" />
		<intercept-url pattern="/person/**" access="hasAnyRole('ROLE_USER','ROLE_ADMIN')" />
		<intercept-url pattern="/resources/**" access="permitAll" />
		
		<!-- Override default login and logout pages -->		
		<form-login login-processing-url="/j_spring_security_check"
			login-page="/loginPage" default-target-url="/login/loginPage"
			username-parameter="j_username" password-parameter="j_password"
			authentication-failure-url="/loginPage?login_error=1" />
		<logout logout-url="/logout" invalidate-session="true"
			success-handler-ref="customLogoutHandler" />
		<logout delete-cookies="JSESSIONID" />
		<!-- enable csrf protection -->
		<csrf disabled="true" />
	</http>

	<security:authentication-manager alias="authenticationManager">
		<security:authentication-provider
			ref="customAuthenticationProvider" />
	</security:authentication-manager>
</beans:beans>
